- Thực hiện build 1 app bằng streamlit có chức năng chat LLM bằng cách gọi API từ openAI

- Đóng gói app bằng Docker (đóng gói image thật nhẹ với multi-stage build)

- .gitignore file .env

- Dockerignore file .env

- Set up environment variables OPENAI_API_KEY trong docker

- Set up CI/CD bằng GitHub Actions

- Set up CD bằng Railway

- Thực hiện JWT authentication với admin và user

- Chức năng đăng nhập với JWT token
